<ul class="pager">
    {% for p in range(1, nbPages) %}
        <li{% if p == page %} class="active"{% endif %}>
            <a href="{{ path(nom, {'departement': departement,'page': p}) }}">{{ p }}</a>
        </li>
    {% endfor %}
</ul>
